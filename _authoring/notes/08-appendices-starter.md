# Appendix A — The Starter Kit

## Status: Outlined

Pre-configured Cloudflare Workers project that readers clone and run. Includes Hono, DaisyUI, HTMX, CLAUDE.md, MCP configs, AI Dev Intro Skill, example prototype, D1 schema templates, deploy scripts.

```
git clone [repo]
cd fullstack-ai-starter
claude
```

## Resources

Drop starter project templates, example configs, etc. into `resources/`.
